spring:
  datasource:
    url: jdbc:mysql://localhost:3306/EmployeeApp
    username: root
    password: root
  jpa:
    properties:
      hibernate:
      dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
server:
  port: 8082      